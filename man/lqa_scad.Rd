% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lqa_scad.R
\name{lqa_scad}
\alias{lqa_scad}
\title{Local Quadratic Approximation (LQA) for SCAD Penalized Regression}
\usage{
lqa_scad(
  y,
  X,
  lambda,
  a = 3.7,
  beta_init = NULL,
  max_iter = 100,
  tol = 1e-06,
  standardize = TRUE
)
}
\arguments{
\item{y}{Response vector of length n}

\item{X}{Design matrix of dimension n x p}

\item{lambda}{Tuning parameter for SCAD penalty (scalar)}

\item{a}{Shape parameter for SCAD penalty, must be > 2 (default: 3.7)}

\item{beta_init}{Initial coefficient vector of length p (default: LASSO solution)}

\item{max_iter}{Maximum number of iterations (default: 100)}

\item{tol}{Convergence tolerance (default: 1e-6)}

\item{standardize}{Logical, whether to standardize X (default: TRUE)}
}
\value{
A list containing:
\item{beta}{Estimated coefficient vector}
\item{iterations}{Number of iterations until convergence}
\item{converged}{Logical indicating convergence}
\item{objective}{Final objective value}
\item{objective_history}{History of objective values}
}
\description{
Implements the LQA algorithm for SCAD penalized regression.
LQA uses quadratic approximation instead of linear approximation (LLA).
}
\details{
Algorithm:
At iteration t, approximate SCAD penalty by quadratic:
p_SCAD(|beta_j|) ≈ p_SCAD(|beta_j^(t)|) + p'_SCAD(|beta_j^(t)|) / (2|beta_j^(t)|) * (beta_j^2 - beta_j^(t)^2)

This leads to solving weighted Ridge regression at each iteration:
argmin ||y - X*beta||^2 + lambda * sum(w_j * beta_j^2)
where w_j = p'_SCAD(|beta_j^(t)|) / |beta_j^(t)|
}
