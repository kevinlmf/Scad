% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lqa_scad_improved.R
\name{lqa_scad_improved}
\alias{lqa_scad_improved}
\title{Stabilized LQA for SCAD Penalized Regression}
\usage{
lqa_scad_improved(
  y,
  X,
  lambda,
  a = 3.7,
  beta_init = NULL,
  max_iter = 100,
  tol = 1e-06,
  standardize = TRUE,
  decomposition = c("qr", "svd", "cholesky"),
  ridge_coef = 0.001,
  svd_threshold = 1e-10
)
}
\arguments{
\item{y}{Response vector of length n}

\item{X}{Design matrix of dimension n x p}

\item{lambda}{Tuning parameter for SCAD penalty (scalar)}

\item{a}{Shape parameter for SCAD penalty, must be > 2 (default: 3.7)}

\item{beta_init}{Initial coefficient vector of length p (default: LASSO estimate)}

\item{max_iter}{Maximum number of iterations (default: 100)}

\item{tol}{Convergence tolerance (default: 1e-6)}

\item{standardize}{Logical, whether to standardize X (default: TRUE)}

\item{decomposition}{Method for stable matrix inversion: "qr", "svd", or "cholesky" (default: "qr")}

\item{ridge_coef}{Coefficient for adaptive ridge term (default: 0.01)}

\item{svd_threshold}{Threshold for singular values in SVD (default: 1e-10)}
}
\value{
A list containing:
\item{beta}{Estimated coefficient vector}
\item{iterations}{Number of iterations until convergence}
\item{converged}{Logical indicating convergence}
\item{objective}{Final objective value}
\item{objective_history}{History of objective values}
\item{ridge_history}{History of adaptive ridge terms}
}
\description{
Innovation 3: Stabilized-LQA using QR/SVD decomposition
}
\details{
The original LQA directly solves (X^T X + W^(t))^(-1), which can be
numerically unstable in high-dimensional settings (p >> n).

This improved version uses:
- QR decomposition or SVD regularization
- Adaptive Ridge boosting: H^(t) = X^T X + W^(t) + γI
- Adaptive ridge term: γ = c * ||W^(t)||_F / sqrt(p)

Key advantages:
- Numerically stable in p >> n settings
- Much more stable than LLA in high dimensions
- Better MSE than LLA, especially with high correlation
}
